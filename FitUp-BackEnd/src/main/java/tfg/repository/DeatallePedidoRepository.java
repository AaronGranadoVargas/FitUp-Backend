package tfg.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import tfg.model.DetallePedido;

public interface DeatallePedidoRepository extends JpaRepository<DetallePedido, Long> {
}
